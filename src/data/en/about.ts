import type { AboutData } from '../types';

export const aboutData: AboutData = {
	content: "Computer Engineer with experience in web (React, Node.js), mobile (Expo) and data science (Python). I adapt quickly, write clean code and focus on delivering results. Currently at BiofMat, open to new opportunities."
};
