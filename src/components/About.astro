---
import { aboutData } from '../data/about';

interface Props {
	content?: string;
}

const { content = aboutData.content } = Astro.props;
---

<section class="max-w-3xl mx-auto text-center mb-24" data-animate="section">
	<div class="flex items-center justify-center mb-8">
		<div class="h-px w-12 bg-gray-800 mr-4"></div>
		<h2 class="text-xs uppercase tracking-widest text-gray-500">Sobre mí</h2>
		<div class="h-px w-12 bg-gray-800 ml-4"></div>
	</div>
	<p class="text-gray-400 leading-relaxed text-sm md:text-base">
		{content}
	</p>
</section>
